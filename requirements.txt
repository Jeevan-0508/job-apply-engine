# Pinned to the exact versions verified working on Python 3.13.
# Unpinned installs let a rebuild pull an untested major (reportlab 4 -> 5),
# which is how a working deploy breaks without a single code change.
streamlit==1.63.0
requests==2.34.2
beautifulsoup4==4.15.0
pdfplumber==0.11.10
python-docx==1.2.0
reportlab==5.0.1
