__pycache__/
*.pyc
.venv/
venv/
resumes/*.pdf
applications/
data/
.streamlit/
.venv/
# Real profile data. The loader reads this locally and a PROFILE_JSON
# secret when hosted, so it must never be committed.
config/profile.py
secrets.toml
